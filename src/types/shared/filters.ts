/**
 * Digunakan untuk menyembunyikan komponen
 *
 * @type object
 */
export type Filters = {
  /** Nama component */
  name: string
  /** Sembunyikan secara otomatis, default: `false` */
  autohide?: boolean
}
